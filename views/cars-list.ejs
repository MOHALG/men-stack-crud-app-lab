<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cars List</title>
</head>
<body>
    <h1>All Cars</h1>
    <ul>
        <% cars.forEach(function(car) { %>
            <a href="/cars/<%= car._id %>"><li><%= car.make %> <%= car.model %> (<%= car.year %>)</li></a>
               
        <% }); %>
    </ul>
    <a href="/cars/new">Create New Car</a>
</body>
</html>
